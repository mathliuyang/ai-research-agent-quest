# Prompts

```text
请先阅读当前 Python 项目，不要立刻修改。

目标：修复实验数据汇总脚本，让它能读取 data/experiment_log.csv，输出 results/summary.csv 和 results/trend.txt。

请先告诉我：
1. 项目结构和入口文件；
2. 当前报错或潜在 bug；
3. 最小修改方案；
4. 运行验证命令。

确认后再改文件。不要改变 CSV 字段含义。
```

```text
请执行最小修改，并运行脚本验证。若出现报错，请根据报错继续迭代，直到输出文件存在且内容合理。
```
